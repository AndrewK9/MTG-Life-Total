Messages from clients:
	CLIENT{
		K:1, //0 is join, 1 is host
		N:text, //users name
		L:text, //users initials

		if(K == 1) U:int //if host, what is starting life

		if(gameOver) R:1 //0 is no restart, 1 is restart
	}

Messages from server:
	SERVER{
		S:0, //0 means lobby, 1 means in game
		U:ing //what the starting life is

		Broadcast user joins
		Broadcast usser leaves
		Broadcast HP update
		Broadcast Infect update
		Broadcast Loss
		Broadcast Win
	}